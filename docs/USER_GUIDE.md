# คู่มือการใช้งาน (User Guide)

**Screen Translation Overlay** - โปรแกรมแปลภาษาบนหน้าจอในเวลาจริง

เวอร์ชัน: 0.1.0
อัปเดตล่าสุด: 31 มกราคม 2025

---

## สารบัญ

1. [แนะนำโปรแกรม](#แนะนำโปรแกรม)
2. [การติดตั้ง](#การติดตั้ง)
3. [การเริ่มต้นใช้งาน](#การเริ่มต้นใช้งาน)
4. [การแปลภาษาบนหน้าจอ](#การแปลภาษาบนหน้าจอ)
5. [การตั้งค่า](#การตั้งค่า)
6. [ประวัติการแปล](#ประวัติการแปล)
7. [ปุ่มลัด (Keyboard Shortcuts)](#ปุ่มลัด-keyboard-shortcuts)
8. [การแก้ไขปัญหา](#การแก้ไขปัญหา)
9. [คำถามที่พบบ่อย (FAQ)](#คำถามที่พบบ่อย-faq)

---

## แนะนำโปรแกรม

**Screen Translation Overlay** เป็นแอปพลิเคชันสำหรับแปลข้อความบนหน้าจอคอมพิวเตอร์เป็นภาษาไทยในเวลาจริง ช่วยให้คุณเข้าใจเนื้อหาภาษาต่างประเทศได้อย่างรวดเร็ว

### จุดเด่นหลัก

- ⚡ **รวดเร็ว**: แปลภาษาได้ภายใน 2-3 วินาที
- 🎯 **แม่นยำ**: รู้จักตัวอักษรความแม่นยำ 90%+ สำหรับข้อความที่ชัดเจน
- 🖱️ **ใช้งานง่าย**: แค่กดปุ่มลัดและลากเลือกบริเวณที่ต้องการ
- 💪 **เบาเครื่อง**: ใช้ทรัพยากรระบบน้อย ไม่ทำให้เครื่องช้า
- 🎨 **ปรับแต่งได้**: เปลี่ยนขนาด สี และตำแหน่งของการแสดงผลได้

### ใครควรใช้โปรแกรมนี้?

- เกมเมอร์ที่เล่นเกมภาษาต่างประเทศ
- ผู้ใช้งานซอฟต์แวร์ที่ไม่รองรับภาษาไทย
- นักเรียน/นักวิจัยที่ต้องอ่านเนื้อหาภาษาต่างประเทศ
- ใครก็ตามที่ต้องการแปลข้อความบนหน้าจอ

---

## การติดตั้ง

### ความต้องการของระบบ

**Windows:**
- Windows 10 หรือ Windows 11 (64-bit)
- RAM 4GB ขึ้นไป
- พื้นที่ว่าง 500MB

**macOS:**
- macOS 11 (Big Sur) ขึ้นไป
- RAM 4GB ขึ้นไป
- พื้นที่ว่าง 500MB

**Linux:**
- Ubuntu 20.04+ หรือ Debian 11+
- RAM 4GB ขึ้นไป
- พื้นที่ว่าง 500MB

### ขั้นตอนการติดตั้ง

#### Windows

1. ดาวน์โหลดไฟล์ติดตั้ง `.exe`
2. ดับเบิลคลิกไฟล์เพื่อเริ่มการติดตั้ง
3. เลือกโฟลเดอร์ที่ต้องการติดตั้ง
4. คลิก "ติดตั้ง" แล้วรอจนเสร็จสิ้น
5. เปิดโปรแกรมจาก Desktop หรือ Start Menu

#### macOS

1. ดาวน์โหลดไฟล์ `.dmg`
2. เปิดไฟล์แล้วลากไอคอนโปรแกรมไปยังโฟลเดอร์ Applications
3. เปิดโปรแกรมจาก Launchpad หรือ Applications
4. ถ้าแจ้งเตือนความปลอดภัย ให้คลิก "เปิด" ใน System Preferences

#### Linux

1. ดาวน์โหลดไฟล์ `.AppImage` หรือ `.deb`
2. สำหรับ AppImage: ดับเบิลคลิกหรือสั่ง `./ScreenTranslationOverlay.AppImage`
3. สำหรับ Debian: `sudo dpkg -i screen-translation-overlay_*.deb`
4. เปิดโปรแกรมจาก Application Menu

---

## การเริ่มต้นใช้งาน

### การเปิดโปรแกรมครั้งแรก

เมื่อเปิดโปรแกรมครั้งแรก คุณจะเห็น:

1. **ไอคอนใน Tray/Menu Bar** - ไอคอนโปรแกรมจะปรากฏที่มุมล่างขวา (Windows/Linux) หรือมุมบนขวา (macOS)
2. **หน้าต่างต้อนรับ** - แนะนำฟีเจอร์หลัก
3. **การตั้งค่าเริ่มต้น** - โปรแกรมจะใช้ค่าเริ่มต้นที่เหมาะสมที่สุด

### เริ่มแปลภาษาทันที

**วิธีที่ง่ายที่สุด:**

1. กดปุ่มลัด `Ctrl + Shift + X` (Windows/Linux) หรือ `Cmd + Shift + X` (macOS)
2. ลากเมาส์เพื่อเลือกบริเวณที่ต้องการแปล
3. ปล่อยเมาส์เพื่อยืนยัน
4. รอสักครู่ ข้อความแปลภาษาไทยจะปรากฏบนหน้าจอ!

---

## การแปลภาษาบนหน้าจอ

### ขั้นตอนแบบละเอียด

#### 1. เริ่มการจับภาพ

กดปุ่มลัด `Ctrl + Shift + X` (Windows/Linux) หรือ `Cmd + Shift + X` (macOS)

- หน้าจอจะจางลงเล็กน้อย
- เคอร์เซอร์จะเปลี่ยนเป็นเครื่องหมาย +
- สามารถกด `Esc` เพื่อยกเลิกได้ตลอดเวลา

#### 2. เลือกบริเวณที่ต้องการ

1. คลิกที่จุดเริ่มต้นของบริเวณที่ต้องการ
2. ลากเมาส์ไปยังจุดสิ้นสุด
3. ปล่อยเมาส์เพื่อยืนยันการเลือก

**ข้อแนะนำ:**
- เลือกเฉพาะบริเวณที่มีข้อความที่ต้องการแปล
- หลีกเลี่ยงพื้นที่ที่ซับซ้อนหรือมีรูปภาพมาก
- ข้อความควรมีความชัดเจนและสีตัดกับพื้นหลัง

#### 3. ประมวลผล

หลังจากปล่อยเมาส์ โปรแกรมจะทำงานอัตโนมัติ:

1. **OCR (รู้จักตัวอักษร)** - แปลงภาพเป็นข้อความ (1-2 วินาที)
2. **แปลภาษา** - แปลเป็นภาษาไทย (น้อยกว่า 1 วินาที)
3. **แสดงผล** - แสดงผลลัพธ์บนหน้าจอ

#### 4. อ่านผลลัพธ์

ข้อความแปลจะปรากฏในหน้าต่างโปร่งใส:

- **ตำแหน่ง**: ใกล้เคอร์เซอร์ (สามารถปรับเปลี่ยนได้)
- **ระยะเวลา**: ปรากฏอยู่ 30 วินาที (สามารถปรับเปลี่ยนได้)
- **ซ่อน**: กด `Ctrl + Shift + H` เพื่อซ่อนทันที

---

## การตั้งค่า

เข้าถึงการตั้งค่าได้จาก:

- คลิกขวาที่ไอคอน Tray → เลือก "การตั้งค่า"
- กดปุ่มลัด `Ctrl + Shift + S` (Windows/Linux) หรือ `Cmd + Shift + S` (macOS)

### ทั่วไป (General)

**ภาษาต้นฉบับ:**
- `Auto` - ตรวจจับอัตโนมัติ (แนะนำ)
- `English` - ภาษาอังกฤษ
- `Japanese` - ภาษาญี่ปุ่น
- `Korean` - ภาษาเกาหลี
- `Chinese (Simplified)` - ภาษาจีนตัวย่อ
- `Chinese (Traditional)` - ภาษาจีนตัวเต็ม

**ธีม:**
- `Light` - ธีมสว่าง
- `Dark` - ธีมมืด (แนะนำสำหรับ Overlay)
- `System` - ทำตามระบบ

**เริ่มอัตโนมัติ:**
- เปิดโปรแกรมอัตโนมัติเมื่อเปิดคอมพิวเตอร์

### ปุ่มลัด (Shortcuts)

คลิกที่ช่องปุ่มลัด แล้วกดปุ่มที่ต้องการเพื่อเปลี่ยน

**ปุ่มลัดเริ่มต้น:**

| ฟังก์ชัน | Windows/Linux | macOS |
|-----------|---------------|-------|
| แปลภาษา | `Ctrl + Shift + X` | `Cmd + Shift + X` |
| ซ่อน Overlay | `Ctrl + Shift + H` | `Cmd + Shift + H` |
| ประวัติ | `Ctrl + Shift + H` | `Cmd + Shift + H` |
| การตั้งค่า | `Ctrl + Shift + S` | `Cmd + Shift + S` |
| ออกจากโปรแกรม | `Ctrl + Shift + Q` | `Cmd + Shift + Q` |

### การแสดงผล (Overlay)

**ความโปร่งใส:** 0-100%
- ค่ามาก = โปร่งใสมาก
- แนะนำ: 90%

**ขนาดตัวอักษร:** 12-32px
- แนะนำ: 16-18px

**สี:**
- สีพื้นหลัง - เลือกจาก Color Picker
- สีตัวอักษร - เลือกจาก Color Picker

**ตำแหน่ง:**
- `เคอร์เซอร์` - ใกล้ตำแหน่งเมาส์ (เริ่มต้น)
- `กลางหน้าจอ` - กึ่งกลางหน้าจอ
- `กำหนดเอง` - ระบุพิกัด X, Y

**ซ่อนอัตโนมัติ:** 0-60 วินาที
- `0` = ไม่ซ่อนอัตโนมัติ
- `30` = ซ่อนหลัง 30 วินาที (เริ่มต้น)

**คลิกผ่าน:**
- เปิด = สามารถคลิกผ่าน Overlay ได้
- ปิด = Overlay จะบล็อกการคลิก

### OCR (รู้จักตัวอักษร)

**ความมั่นใจขั้นต่ำ:** 0-100%
- กรองข้อความที่มีความมั่นใจต่ำกว่าค่าที่กำหนด
- แนะนำ: 70%

**ภาษา:** เหมือนกับ "ภาษาต้นฉบับ" ในทั่วไป

### การแปล

**ผู้ใช้บริการ:**
- `Google Translate` - ใช้งานฟรี, แม่นยำดี
- (อนาคต) `DeepL` - แม่นยำกว่า แต่ต้องจ่ายเงิน

**ใช้แคช:**
- เปิด = จดจำการแปลซ้ำ เร็วขึ้น
- แนะนำ: เปิด

---

## ประวัติการแปล

### เข้าถึงประวัติ

- คลิกขวาที่ไอคอน Tray → "ประวัติ"
- กดปุ่มลัด `Ctrl + Shift + H` (Windows/Linux) หรือ `Cmd + Shift + H` (macOS)

### ค้นหา

พิมพ์คำค้นในช่องค้นหา:

- ค้นหาจากข้อความต้นฉบับ
- ค้นหาจากข้อความแปล
- ค้นหาได้ทันทีขณะพิมพ์

### จัดการรายการ

**คัดลอก:**
- คลิกที่รายการ เพื่อคัดลอกข้อความแปล
- หรือคลิกขวา → "คัดลอกข้อความแปล"

**ลบรายการ:**
- คลิกที่ปุ่ม × ที่รายการ
- หรือคลิกขวา → "ลบ"

**ล้างประวัติ:**
- คลิกปุ่ม "ล้างประวัติทั้งหมด"
- ยืนยันการลบ

**ส่งออก:**
- คลิกปุ่ม "ส่งออก"
- เลือกรูปแบบ: JSON หรือ CSV
- เลือกตำแหน่งบันทึกไฟล์

---

## ปุ่มลัด (Keyboard Shortcuts)

### ปุ่มลัดหลัก

| ฟังก์ชัน | Windows/Linux | macOS | คำอธิบาย |
|-----------|---------------|-------|-------------|
| **แปลภาษา** | `Ctrl + Shift + X` | `Cmd + Shift + X` | เริ่มการจับภาพและแปล |
| **ซ่อน Overlay** | `Ctrl + Shift + H` | `Cmd + Shift + H` | ซ่อนหน้าต่างผลลัพธ์ |
| **แสดงประวัติ** | `Ctrl + Shift + H` | `Cmd + Shift + H` | เปิดหน้าต่างประวัติ |
| **การตั้งค่า** | `Ctrl + Shift + S` | `Cmd + Shift + S` | เปิดหน้าต่างการตั้งค่า |
| **ออกจากโปรแกรม** | `Ctrl + Shift + Q` | `Cmd + Shift + Q` | ปิดโปรแกรม |

### ปุ่มลัดในหน้าต่าง

**หน้าต่างการจับภาพ:**
- `Esc` - ยกเลิกการจับภาพ
- `Enter` - ยืนยันบริเวณที่เลือก

**หน้าต่างประวัติ:**
- `Ctrl/Cmd + F` - โฟกัสที่ช่องค้นหา
- `Delete` - ลบรายการที่เลือก
- `Ctrl/Cmd + A` - เลือกทั้งหมด
- `Esc` - ปิดหน้าต่าง

**หน้าต่างการตั้งค่า:**
- `Ctrl/Cmd + 1` - แท็บทั่วไป
- `Ctrl/Cmd + 2` - แท็บปุ่มลัด
- `Ctrl/Cmd + 3` - แท็บการแสดงผล
- `Ctrl/Cmd + 4` - แท็บประวัติ

---

## การแก้ไขปัญหา

### ปัญหา: ไม่สามารถจับภาพหน้าจอได้

**อาการ:** กดปุ่มลัดแล้วไม่เกิดอะไรขึ้น

**วิธีแก้:**
1. ตรวจสอบสิทธิ์การเข้าถึงหน้าจอ:
   - **macOS**: System Preferences → Security & Privacy → Privacy → Screen Recording → ให้สิทธิ์โปรแกรม
   - **Windows**: ติดตั้ง .NET Desktop Runtime หากจำเป็น
2. รีสตาร์ทโปรแกรม
3. ถ้ายังไม่ได้ รีสตาร์ทคอมพิวเตอร์

---

### ปัญหา: แปลผิดหรือไม่แม่นยำ

**อาการ:** ผลลัพธ์แปลไม่ตรงหรืออ่านไม่รู้เรื่อง

**วิธีแก้:**
1. **ตรวจสอบภาษาต้นฉบับ** - ตั้งค่าภาษาให้ถูกต้อง
2. **เลือกบริเวณให้ชัดเจน** - หลีกเลี่ยงพื้นหลังที่ซับซ้อน
3. **เพิ่มความมั่นใจ OCR** - ลดค่า "ความมั่นใจขั้นต่ำ" ในการตั้งค่า OCR
4. **ใช้ภาพที่ชัด** - ขยายขนาดข้อความก่อนแปล
5. **รายงานปัญหา** - ส่งภาพตัวอย่างให้ทีมพัฒนาเพื่อปรับปรุง

---

### ปัญหา: แปลช้ามาก

**อาการ:** ใช้เวลานานกว่า 5 วินาที

**วิธีแก้:**
1. **ตรวจสอบอินเทอร์เน็ต** - การแปลต้องใช้อินเทอร์เน็ต
2. **ปิดโปรแกรมอื่น** - ปิดโปรแกรมที่ใช้ CPU/เน็ตมาก
3. **ลดขนาดบริเวณ** - เลือกพื้นที่เล็กลง
4. **ตรวจสอบความเร็ว CPU** - เปิด Task Manager (Windows) หรือ Activity Monitor (macOS)

---

### ปัญหา: ไม่พบข้อความ (No Text Detected)

**อาการ:** แจ้งเตือน "ไม่พบข้อความในภาพ"

**วิธีแก้:**
1. **ตรวจสอบว่ามีข้อความจริงๆ** - ภาพต้องมีตัวอักษร
2. **ปรับความมั่นใจ OCR** - ลดค่าลงเหลือ 60% หรือน้อยกว่า
3. **เลือกภาพที่ชัด** - ข้อความต้องมีความละเอียดสูง
4. **หลีกเลี่ยงภาพที่เอียง** - จับภาพให้ตรงมากที่สุด
5. **เปลี่ยนภาษา OCR** - ลองเปลี่ยนภาษาในการตั้งค่า

---

### ปัญหา: หน้าต่าง Overlay ไม่หาย

**อาการ:** ผลลัพธ์แสดงค้างไว้บนหน้าจอ

**วิธีแก้:**
1. กดปุ่มลัด `Ctrl/Cmd + Shift + H` เพื่อซ่อน
2. คลิกขวาที่ไอคอน Tray → "ซ่อน Overlay"
3. ถ้ายังไม่หาย รีสตาร์ทโปรแกรม

---

### ปัญหา: เครื่องช้าหลังติดตั้ง

**อาการ:** คอมพิวเตอร์ทำงานช้าลง

**วิธีแก้:**
1. **ปิดการเริ่มอัตโนมัติ** - การตั้งค่า → ทั่วไป → ปิด "เริ่มอัตโนมัติ"
2. **ลดจำนวน Worker** - การตั้งค่า → OCR → ลด Worker Count
3. **ตรวจสอบไวรัส** - ตรวจสอบว่าดาวน์โหลดจากแหล่งที่เชื่อถือได้
4. **อัปเดตโปรแกรม** - ใช้เวอร์ชันล่าสุด

---

### ปัญหา: แปลไม่ได้ในขณะออฟไลน์

**อาการ:** ไม่มีอินเทอร์เน็ตแล้วแปลไม่ได้

**คำอธิบาย:** โปรแกรมต้องใช้อินเทอร์เน็ตสำหรับการแปล (Google Translate API)

**วิธีแก้:**
- **ใช้แคช** - โปรแกรมจะจดจำการแปลซ้ำ แปลซ้ำอีกครั้งไม่ต้องใช้เน็ต
- **รอฟีเจอร์ออฟไลน์** - รุ่นอนาคตจะรองรับการแปลแบบออฟไลน์

---

## คำถามที่พบบ่อย (FAQ)

### **คำถามทั่วไป**

**Q: โปรแกรมนี้ฟรีหรือเปล่า?**
A: ใช่ โปรแกรมนี้ฟรีและโอเพนซอร์ส 100% ไม่มีค่าใช้จ่ายแอบแฝง

**Q: ปลอดภัยหรือไม่?**
A: ใช่ โปรแกรมไม่ส่งข้อมูลส่วนตัวของคุณไปไหน การแปลทำเผื่อภายในเครื่อง (OCR) และส่งเฉพาะข้อความที่ต้องการแปลไปยัง Google Translate เท่านั้น

**Q: รองรับภาษาอื่นนอกจากไทยไหม?**
A: ตอนนี้รองรับแปลเป็นภาษาไทยเท่านั้น แต่รองรับภาษาต้นฉบับหลายภาษา (อังกฤษ, ญี่ปุ่น, เกาหลี, จีน)

**Q: ใช้งานบนหลายจอภาพได้ไหม?**
A: ได้! โปรแกรมรองรับหลายจอภาพ (Multi-monitor) สามารถเลือกจับภาพจากจอใดก็ได้

**Q: มีแอปมือถือไหม?**
A: ยังไม่มี แต่เราวางแผนพัฒนาสำหรับ iOS และ Android ในอนาคต

---

### **คำถามเทคนิค**

**Q: ใช้เทคโนโลยีอะไรในการรู้จักตัวอักษร?**
A: ใช้ Tesseract.js - โปรแกรม OCR แบบโอเพนซอร์สที่ทำงานบนเบราว์เซอร์

**Q: แปลภาษาด้วยวิธีไหน?**
A: ใช้ Google Translate API สำหรับการแปล

**Q: ข้อมูลของฉันถูกเก็บไว้ที่ไหน?**
A: ข้อมูลทั้งหมดเก็บในเครื่องคุณเอง (Local Storage):
- การตั้งค่า: `~/.config/screen-translation-overlay/settings.json`
- ประวัติ: `~/.config/screen-translation-overlay/history.db`

**Q: สามารถส่งออกประวัติได้ไหม?**
A: ได้! ไปที่หน้าต่างประวัติ → คลิก "ส่งออก" → เลือกรูปแบบ (JSON หรือ CSV)

**Q: โปรแกรมใช้ทรัพยากรเท่าไหร่?**
A: โดยเฉลี่ย:
- RAM: 100-200 MB เมื่อไม่ได้ใช้งาน
- CPU: <5% เมื่อไม่ได้ใช้งาน
- พื้นที่ดิสก์: ~150 MB

---

### **คำถามเรื่องการใช้งาน**

**Q: จำกัดจำนวนครั้งในการแปลหรือไม่?**
A: ไม่จำกัด! แปลได้ไม่อั้ม แต่ Google อาจมีการจำกัดอัตรา (Rate Limiting) ถ้าใช้มากเกินไปในเวลาสั้นๆ

**Q: แปลภาพได้ไหม?**
A: ได้! โปรแกรมออกแบบมาเพื่อจับภาพหน้าจอ แต่คุณสามารถเปิดรูปภาพแล้วจับภาพนั้นได้

**Q: รองรับภาษาไทยเป็นภาษาต้นฉบับไหม?**
A: ได้ แต่การแปลจะเป็นภาษาไทยเป้าหมายเสมอ

**Q: ปรับแต่งตำแหน่ง Overlay ได้ไหม?**
A: ได้! ไปที่ การตั้งค่า → การแสดงผล → ตำแหน่ง → เลือก "กำหนดเอง"

**Q: เปลี่ยนปุ่มลัดได้ไหม?**
A: ได้! ไปที่ การตั้งค่า → ปุ่มลัด → คลิกที่ช่องปุ่มลัดที่ต้องการ → กดปุ่มใหม่

---

### **คำถามเรื่องปัญหา**

**Q: ทำไมแปลผิด?**
A: อาจเป็นเพราะ:
1. ภาษาต้นฉบับตั้งค่าผิด
2. ภาพไม่ชัด
3. ตัวอักษรซับซ้อนหรือมีสไตล์พิเศษ
4. พื้นหลังซับซ้อน

**Q: ทำไม OCR อ่านไม่ออก?**
A: ลอง:
1. เลือกบริเวณที่ชัดเจน
2. ขยายขนาดข้อความ
3. เพิ่มแสง/ความคมชัด
4. ลดค่า "ความมั่นใจขั้นต่ำ"

**Q: โปรแกรมค้าง (Crash) ทำไง?**
A:
1. รีสตาร์ทโปรแกรม
2. รายงานปัญหาที่ GitHub Issues
3. อัปเดตเวอร์ชันล่าสุด

---

### **คำถามเรื่องอนาคต**

**Q: จะมีฟีเจอร์อะไรเพิ่มในอนาคต?**
A: เราวางแผน:
- DeepL และผู้ให้บริการแปลอื่น
- แปลหลายภาษาเป้าหมาย (ไม่ใช่แค่ไทย)
- แปลแบบออฟไลน์
- แอปมือถือ
- ปลั๊กอินสำหรับเบราว์เซอร์

**Q: ฉันจะร่วมพัฒนาได้ไหม?**
A: ได้แน่นอน! โปรเจกต์นี้โอเพนซอร์ส:
- GitHub: [link]
- อ่านเอกสารพัฒนา: `docs/DEVELOPMENT.md`

**Q: จะรายงานบั๊กได้ที่ไหน?**
A: ไปที่ GitHub Issues: [link]

**Q: จะขอฟีเจอร์ใหม่ได้ไหม?**
A: ได้! สร้าง Issue ใหม่ที่ GitHub พร้อมป้ายกำกับ "enhancement"

---

## ติดต่อเรา

- **Website**: [Coming Soon]
- **GitHub**: [link]
- **Email**: [support@email]
- **Discord**: [link]

---

ขอบคุณที่ใช้ **Screen Translation Overlay** 🙏

หากคู่มือนี้มีประโยชน์ และมีข้อเสนอแนะใดๆ โปรดบอกเรา!
